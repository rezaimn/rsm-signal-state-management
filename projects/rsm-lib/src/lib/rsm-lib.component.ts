import { Component, inject } from '@angular/core';
import {  RsmLibService } from './rsm-lib.service';

@Component({
  selector: 'lib-rsm-lib',
  template: `
    <p>
      rsm-lib works!
    </p>
  `,
  styles: [
  ]
})
export class RsmLibComponent {
 
}
